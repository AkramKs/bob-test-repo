# Architecture

> Generated by BobTheDocker · 2026-06-27

# Architecture Reference

## Tech Stack & Dependencies
- **Python 3** (inferred)
- **FastAPI** (`0.104.1`) – web framework
- **Uvicorn** (`0.24.0`) – ASGI server
- **SQLAlchemy** (`2.0.23`) – ORM
- **Pydantic** (`2.5.2`) – data validation
- **SQLite** – database (file `grocery_store.db` present)
- **pytest** (`7.4.3`) – test runner
- **HTTPX** (`0.25.2`) – async HTTP client for testing

## Key Directories & What Lives Where
| Directory/File | Purpose |
|----------------|---------|
| `main.py` | FastAPI application entry point – creates app, defines CRUD endpoints, serves static files. |
| `models.py` | SQLAlchemy ORM models (database schema). |
| `schemas.py` | Pydantic models for request/response validation. |
| `hello.py` | Additional module/route (possibly a simple greeting endpoint). |
| `grocery_store.db` | SQLite database file (committed, likely for development). |
| `test_main.py` | Tests for main API endpoints using pytest + httpx. |
| `test_hello.py` | Tests for `hello.py` functionality. |
| `static/` | Frontend assets: `index.html`, `js/app.js`, `css/styles.css`. Served by FastAPI at `/static`. |
| `requirements.txt` | Python dependencies. |
| `README.md` | Project overview, usage, and API documentation. |

## Entry Points
- **Web server**: `uvicorn main:app --reload` → starts FastAPI app defined in `main.py`.
- **API docs**: Swagger UI at `/docs` (auto‑generated by FastAPI).
- **Frontend**: `static/index.html` – likely a simple SPA that consumes the API.

## Testing
- **Framework**: pytest
- **HTTP client**: HTTPX (async test client for FastAPI)
- **Run tests**: `pytest -v` (executes `test_main.py` and `test_hello.py`)
- Tests are co‑located in project root; they import the application using FastAPI’s `TestClient`.

## Build & Run Commands
- Install dependencies: `pip install -r requirements.txt`
- Run locally: `uvicorn main:app --reload`
- No separate build step – Python project, no Dockerfile or database migration scripts evident.

## Notable Patterns & Conventions
- **Layered architecture**: SQLAlchemy models (`models.py`) and Pydantic schemas (`schemas.py`) separated from route handlers.
- **CRUD pattern**: Standard RESTful endpoints for products (POST, GET, PUT, DELETE).
- **Static file serving**: FastAPI’s `StaticFiles` middleware is likely mounted at `/static` to deliver frontend assets.
- **Database first**: `grocery_store.db` is pre‑existing; SQLAlchemy `Base.metadata.create_all` may be present but not necessarily used in production.
- **Optional product attributes**: Partial updates allowed (PATCH‑like behavior via PUT).
- **Testing uses `TestClient` from FastAPI** and likely makes real HTTP requests to a live (temporary) application instance.
